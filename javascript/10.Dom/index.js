const objectHeader = document.querySelector('.header');

console.log(objectHeader.textContent);

// thay đổi nội du g thẻ
objectHeader.textContent = 'header update';

console.log(objectHeader.textContent);
